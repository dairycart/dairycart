sudo: required

language: go

services:
  - docker

go:
  - "1.10.x"

script:
  - make tests
  - make ci-coverage

after_success:
  # send coverage report to codecov
  - bash <(curl -s https://codecov.io/bash)